/**
 * unlimited-lp template — standalone landing page.
 * The global site header and footer are hidden via unlimited-lp.css
 * (body.unlimited-lp > header/footer { display: none }). We deliberately
 * leave the elements in the DOM so the boilerplate loadHeader()/loadFooter()
 * routines can run without error; they simply render into hidden containers.
 * @param {Element} main the page main element passed by loadTemplate
 */
export default function decorate() {
  // No DOM changes needed — styling handles chrome removal.
}
